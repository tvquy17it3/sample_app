<li>
  <%= gravatar_for user, size: 50 %>
  <%= link_to user.name, user %>
  <% if is_admin?(user) %>
   |   <%= link_to "delete", user, method: :delete, data: {confirm: t("you_sure")} %>
  <% end %>
</li>
